{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "chimera-api",
  "projectType": "application",
  "root": "apps/vuln-api",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/vuln-api",
        "command": "just test-ci"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/vuln-api",
        "command": "PORT=8880 just run"
      }
    },
    "stop": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/vuln-api",
        "command": "printf 'chimera-api:start runs in the foreground; stop it with Ctrl+C or terminate the process externally.\\n'"
      }
    },
    "serve-api-demo": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/vuln-api",
        "command": "node scripts/run-compose.mjs up api-demo"
      }
    },
    "stop-api-demo": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/vuln-api",
        "command": "node scripts/run-compose.mjs down api-demo"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["vuln-web:build:bundle"],
      "options": {
        "cwd": "apps/vuln-api",
        "command": "uv build"
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "apps/vuln-api",
        "command": "uv publish"
      }
    }
  }
}
